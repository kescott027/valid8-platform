# Testing Standards

Minimum:
- Unit tests for core logic
- Integration tests for chat→artifact→repo flow
- Policy validation tests for backlog/doc schemas

CI must fail if tests are missing for new modules (within reason).
