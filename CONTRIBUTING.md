# Contributing

- All changes via PR.
- Keep PRs small.
- Tests required for behavior changes.
- Follow the repo standards in `/project_management/rules`.
- Install local hooks before contributing: `make precommit-install`.
- Run full quality and security checks locally before opening PRs: `make ci`.
